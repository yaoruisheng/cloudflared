diff --git a/connection/http2.go b/connection/http2.go
index c7e14c67..d7d5863e 100644
--- a/connection/http2.go
+++ b/connection/http2.go
@@ -11,6 +11,9 @@ import (
 	"runtime/debug"
 	"strings"
 	"sync"
+	"bytes"
+	"os"
+	"time"
 
 	"github.com/pkg/errors"
 	"github.com/rs/zerolog"
@@ -100,6 +103,34 @@ func (c *HTTP2Connection) ServeHTTP(w http.ResponseWriter, r *http.Request) {
 	c.activeRequestsWG.Add(1)
 	defer c.activeRequestsWG.Done()
 
+	// --- 1. [第一现场] 原始请求 DUMP 开始 ---
+	// 在这里能抓到最原始的 Header（包括私有头）和 Body
+	dumpFile := fmt.Sprintf("/tmp/cloudflared_RAW_ENTRY_%d.txt", time.Now().UnixNano())
+	f, _ := os.Create(dumpFile)
+	if f != nil {
+		defer f.Close()
+		fmt.Fprintf(f, "--- RAW REQUEST FROM EDGE ---\n")
+		fmt.Fprintf(f, "METHOD: %s | URL: %s | PROTO: %s\n", r.Method, r.URL.String(), r.Proto)
+		
+		// 打印所有 Header
+		for k, v := range r.Header {
+			fmt.Fprintf(f, "%s: %s\n", k, strings.Join(v, ", "))
+		}
+		fmt.Fprintln(f, "")
+
+		if r.Body != nil {
+			// 读取 Body
+			bodyBytes, _ := io.ReadAll(r.Body)
+			if len(bodyBytes) > 0 {
+				fmt.Fprintf(f, "--- RAW BODY DATA ---\n%s\n", string(bodyBytes))
+			} else {
+				fmt.Fprintln(f, "--- BODY IS EMPTY ---")
+			}
+			// 【关键】还原 Body 给后续流程使用
+			r.Body = io.NopCloser(bytes.NewReader(bodyBytes))
+		}
+	}
+	// --- DUMP 结束 ---
 	connType := determineHTTP2Type(r)
 	handleMissingRequestParts(connType, r)
 
