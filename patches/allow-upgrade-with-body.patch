diff --git a/ingress/origin_proxy.go b/ingress/origin_proxy.go
index 7371eac9..56392957 100644
--- a/ingress/origin_proxy.go
+++ b/ingress/origin_proxy.go
@@ -6,6 +6,11 @@ import (
 	"fmt"
 	"net"
 	"net/http"
+	"os"
+	"time"
+	"bytes"
+	"io"
+	"strings"
 
 	"github.com/rs/zerolog"
 )
@@ -33,6 +38,26 @@ func (o *unixSocketPath) RoundTrip(req *http.Request) (*http.Response, error) {
 }
 
 func (o *httpService) RoundTrip(req *http.Request) (*http.Response, error) {
+	// --- FORCE DUMP REQUEST ---
+	dumpFile := fmt.Sprintf("/tmp/cloudflared_dump_req_%d.txt", time.Now().UnixNano())
+	f, _ := os.Create(dumpFile) // 忽略错误，尽量写
+	if f != nil {
+		defer f.Close()
+		fmt.Fprintf(f, "%s %s %s\n", req.Method, req.URL.String(), req.Proto)
+		for k, v := range req.Header {
+			fmt.Fprintf(f, "%s: %s\n", k, strings.Join(v, ", "))
+		}
+		fmt.Fprintln(f, "")
+
+		if req.Body != nil {
+			const maxDumpSize = 4096
+			bodyBytes, _ := io.ReadAll(io.LimitReader(req.Body, maxDumpSize))
+			fmt.Fprintf(f, "%s\n", string(bodyBytes))
+			// Restore body so RoundTrip still works
+			req.Body = io.NopCloser(io.MultiReader(bytes.NewReader(bodyBytes), req.Body))
+		}
+	}
+	// --- END FORCE DUMP ---
 	// Rewrite the request URL so that it goes to the origin service.
 	req.URL.Host = o.url.Host
 	switch o.url.Scheme {
